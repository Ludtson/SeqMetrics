# Install: tail_hydrophobicity (CTTH)

No install needed. Pure Python standard library (Kyte-Doolittle scale is
a hardcoded lookup table), no external tool, no conda environment. Just
run `modules/tail_hydrophobicity/compute_ctth.py` with any Python 3.
